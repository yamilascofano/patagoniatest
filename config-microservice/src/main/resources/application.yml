server:
  port: 8081
  eureka:
    client:
      fetch-registry: true
      register-with-eureka: true
      service-url:
        default-zone: http://localhost:8761/eureka

spring:
 application:
   name: config-microservice

 cloud:
   config:
     server:
       git:
         default-label: addGateway
         uri: https://github.com/yamilascofano/patagoniatest
         search-paths:
           - config-data




